{
	"paths": {
	},

	"general": {
		"device": "cpu"
	},

	"training": {
		"save_model": false,
		"continue_training": false,
		"epochs": 1000,
		"learning_rate": 0.1,
		"batch_size": 1
	},

	"inference": {
		"save_model": false,
		"continue_inference": false
	},

	"data": {
		"type": "shallow_water",  // "burger", "shallow_water", "diffusion_reaction", "allen_cahn"
		"name": "data_train", // "data_train", "data_ext", "data_test"
		"noise": 0.0
	},

	"model": {
		"name": "shallow_water", // "burger", "allen_cahn", "shallow_water"
		"number":4,  // The i-th model
		"layer_sizes": [2, 5, 10, 5, 1] // [1, 10, 20, 10, 1] for burger, diffusion_sorption, and allen_cahn, [2, 20, 20, 20, 2] for diffusion_reaction
	}
}